{
  "name": "citation-registry-blockchain",
  "version": "1.0.0",
  "description": "Smart contract for academic citation registry on blockchain",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "smart-contracts",
    "academic-citations",
    "hardhat"
  ],
  "author": "Citation Registry Project",
  "license": "MIT"
}